<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bangkit.signforyou">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignForYou">
        <activity android:name=".ui.dictionary.detail.DetailActivity" />
        <activity android:name=".ui.dictionary.DictionaryActivity" />
        <activity android:name=".ui.translation.camera.CameraActivity" />
        <activity
            android:name=".ui.translation.TranslateActivity"
            android:label="@string/title_activity_translate"
            android:theme="@style/Theme.SignForYou.NoActionBar" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>